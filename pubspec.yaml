name: flutter_blue_background
description: Flutter Blue Background enables BLE functionality in the background for Android and iOS.
version: 0.0.2
homepage: https://github.com/sparkleo-io/flutter_blue_background.git

# Wide constraints so the package can be consumed by older Flutter/Dart apps
# as well as the latest stable. Keep dependency floors low for the same reason.
environment:
  sdk: '>=3.0.0 <5.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.sparkleo.flutter_blue_background.flutter_blue_background
        pluginClass: FlutterBlueBackgroundPlugin
      ios:
        pluginClass: FlutterBlueBackgroundPlugin
